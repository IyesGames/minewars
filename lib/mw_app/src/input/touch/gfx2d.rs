use crate::prelude::*;
use super::*;

pub struct Gfx2dTouchInputPlugin;

impl Plugin for Gfx2dTouchInputPlugin {
    fn build(&self, app: &mut App) {
        // app.add_systems(Update, (
        // ));
    }
}
