use crate::prelude::*;
use super::*;

pub struct Gfx2dKeyboardInputPlugin;

impl Plugin for Gfx2dKeyboardInputPlugin {
    fn build(&self, app: &mut App) {
        // app.add_systems(Update, (
        // ));
    }
}
